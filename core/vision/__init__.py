"""Vision: camera/LiDAR inference, depth measurement, and person tracking.

Named ``vision`` (not ``perception``) to avoid shadowing
``sim/isaac/perception`` -- both ``core/`` and ``sim/isaac/`` sit on the
Isaac process sys.path, so a same-named subpackage would collide.
"""
