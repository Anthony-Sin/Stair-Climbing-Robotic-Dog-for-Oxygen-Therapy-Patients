"""Internal package for launcher.py's extracted parts.

This is a pure structural split of the top-level ``launcher.py`` module. The
public/private names still live on ``launcher`` (the facade re-exports them);
these submodules only exist to keep each file cohesive and under length.
"""
