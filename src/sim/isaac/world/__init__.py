"""Scene-world actors owned alongside the Isaac environment.

Members:
  - sim_go2_locomotion  staircase geometry (StairSpec / presets / configure_stairs) +
                        synthetic "stair_demo" HUD telemetry (decoration only)
  - sim_person_actor    spawns and animates the simulated patient (BipedMannequin)

Imported via ``from world.<module> import ...`` with ``sim/isaac`` on ``sys.path``.
"""
